# .github/workflows/deploy-log-retrieval.yml
name: Deploy Log Retrieval Lambda
on:
  workflow_dispatch:
  push:
    branches: [workflows]
    paths: [ 'get_log/**' ]
permissions:
  id-token: write
  contents: read
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Install and zip
        working-directory: get_log
        run: |
          pip install -r requirements.txt -t lambda_package
          cp retrieve_logs.py lambda_package/
          cd lambda_package && zip -r ../function.zip .
      - name: Deploy
        run: |
          aws lambda update-function-code \
            --function-name logRetrievalFunction \
            --zip-file fileb://get_log/function.zip \
            --publish
